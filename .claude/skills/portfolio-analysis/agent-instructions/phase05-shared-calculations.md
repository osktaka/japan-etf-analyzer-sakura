# Phase 0.5: 共通定量計算（全モード共通）指示書

## 目的

Phase 1で各アナリストエージェントが重複して行っていた決定論的計算を、Phase 0.5で1回で済ませる。全モード（speed/normal/debate）で実行する。
本エージェントは**計算のみ**を行い、解釈・判断は一切含めない。

## エージェント種別

- subagent_type: general-purpose
- model: sonnet

## データ受け渡し

- **入力**: `{WORK_DIR}/00_portfolio_data.json`
- **出力**: `{WORK_DIR}/05_shared_calculations.md`
- **メインへの戻り値**: 「共通定量計算完了」の1行のみ。データ全文やテーブル全体を返さないこと

---

## 計算ロジック参照元

各計算項目の具体的な計算方法・出力形式は、以下のファイルを確認すること:

- `{skill_dir}/agent-instructions/phase1-quant-analyst.md` — 項目1-4の計算式・出力テーブル形式
- `{skill_dir}/agent-instructions/phase1-score-analyst.md` — 項目5-6の計算式・出力テーブル形式

---

### Pythonスクリプト（必須実行）

- `backend/scripts/phase05_shared_calc.py` は項目1-7の決定論的計算を実行する
- 閾値パラメータは `.claude/skills/portfolio-analysis/calc_params.json` で一元管理
- スクリプト実行後の `_calc_temp.json` を読み込み、サブエージェントが全項目をMarkdownとして `05_shared_calculations.md` に出力する
- **スクリプト実行は必須**。スクリプトの計算結果を手動で再計算してはならない（DO_NOT_RECALCULATE原則）

---

## 計算対象（項目1-6）

### 1. シャープレシオ分析・リスク調整後ランキング

- `00_portfolio_data.json` の `performance_cache` から各銘柄の1年リターン・ボラティリティを取得
- `{WORK_DIR}/0a_market_environment.md` の「## リスクフリーレート」セクションから日本国債10年利回りを取得する。以下のいずれかに該当する場合はフォールバック値 **0.5%** を使用:
  - 該当セクションが存在しない
  - `0a_market_environment.md` が未生成（**Phase 0.5はPhase 0完了時点で起動可能、Phase 0a完了を待たない**）
  - 取得元URLが公的機関（mof.go.jp / boj.or.jp）でない
- 計算式: `(年率リターン - リスクフリーレート) / 年率ボラティリティ`
- 使用したリスクフリーレートの値と出典を出力の冒頭に明記する
- 全銘柄のシャープレシオをランキング表として出力
- シャープレシオ出力には以下の注記を必ず付記する: 「計測期間: 直近1年（YYYY-MM-DD〜YYYY-MM-DD）。期間選択により値は変動する」
- 出力形式は `phase1-quant-analyst.md` の項目A・Eに準拠

### 2. 相関分析

#### 相関分析 閾値定義（他ファイルから参照）
- CORR_HIGH: 0.85（高相関判定）
  - 根拠: ポートフォリオ理論で「高相関」とされる一般的閾値。日本ETF市場では同一インデックス連動ETF間で0.95以上、異なるインデックス間で0.7-0.9が典型的
- CORR_LOW: 0.3（低相関判定）
  - 根拠: 分散効果が有意に期待できる閾値。Markowitz理論に基づき、相関0.3以下で分散投資の恩恵が顕著になる
- RETURN_GAP_THRESHOLD: 15%（年率リターン格差）
  - 根拠: 経験的設定値。高相関ペアでも年率15%以上のリターン格差がある場合、同一資産クラスの「実質重複」とは言えないため除外する目的。日本ETFの実データで検証推奨
- BETA_DIFF_THRESHOLD: 0.3（ベータ差）
  - 根拠: 経験的設定値。ベータ差0.3以上は市場感応度が有意に異なることを示す。リスク・リターン特性の実質的な違いを識別する閾値として設定
- TE_THRESHOLD: 7%（年率追跡誤差 ≒ 月次2% × √12）
  - 根拠: 年率追跡誤差7%は月次約2%（= 7%/√12）に相当。パッシブETFの許容乖離の上限目安。これを超えると異なるリスク特性を持つと判断
- STABILITY_DIFF: 0.3（前後半相関差）
  - 根拠: 経験的設定値。前半・後半の相関差が0.3を超える場合、構造変化やレジームシフトの可能性を示唆する。日本ETFの実データで検証推奨
- MIN_MONTHS_FULL: 14（相関安定性の算出に必要な最小データ点数）
  - 根拠: 前後半分割（各7点以上）で相関係数の統計的信頼性を確保するための最低要件。7点未満の相関係数は自由度不足で不安定
- MIN_MONTHS_BASIC: 6（相関・ベータの算出に必要な最小データ点数）
  - 根拠: 相関係数の統計的信頼性の最低要件。6点未満では自由度不足により相関・ベータの推定が不安定になる
- MIN_HOLDINGS_COMPOSITE: 5（複合判定の最小保有銘柄数）
  - 根拠: 経験的設定値。5銘柄未満では複合判定（5条件分岐）の識別力が低下し、誤分類リスクが高まるため、簡易判定にフォールバックする

#### 市場代理ETFの確定（Phase 0.5冒頭で1回決定・固定）

以下の優先順で市場代理ETFを決定し、出力冒頭に明記する:

1. 1306（TOPIX連動）を保有していれば使用
2. なければ国内インデックスETFのうち信託報酬最低のものを使用
3. いずれも非保有: 全保有銘柄の等加重月次リターン平均を使用
   → この場合ベータ差条件は無効化（同一セクター集中時の誤分類防止）

→ 「市場代理: {コード}（フォールバック: {1/2/3}）」として冒頭に明記

#### 計算内容

1. **相関係数（既存）**: `00_portfolio_data.json` の `price_data` から月初価格を抽出し、月次リターンを計算。全ペアのピアソン相関係数を算出
2. **ベータ**: β_i = ρ_im × (σ_i / σ_m) ← 市場代理を使用。ベータ差 = |β_i - β_j|。MIN_MONTHS_BASIC（6ヶ月）未満のデータの銘柄を含むペアはスキップ（「算出不可」と記録）
3. **年率リターン格差**: |1y_return_i - 1y_return_j|（performance_cacheから差分）
4. **年率追跡誤差**: std(R_it - R_jt) × √12（月次リターン差のSDを年率換算）。MIN_MONTHS_BASIC（6ヶ月）未満のデータの銘柄を含むペアはスキップ
5. **相関安定性**: データ点数 ≥ MIN_MONTHS_FULL（14）の場合のみ計算。|ρ_前半 - ρ_後半|、差 > STABILITY_DIFF（0.3） → 「不安定」。データ点数 < 14: 「算出不可」と記録

#### 複合判定ロジック

**前提条件**: 保有銘柄数 ≥ MIN_HOLDINGS_COMPOSITE（5銘柄）の場合のみ適用。5銘柄未満は複合判定をスキップし「相関+リターン格差の2軸表示」にフォールバック。

**判定順序（この順で評価し最初に一致したラベルを採用）**:

1. **「高リスク連動型」**: 相関 > CORR_HIGH AND (リターン格差 ≥ RETURN_GAP_THRESHOLD OR ベータ差 ≥ 0.5)
2. **「実質重複」**: 相関 > CORR_HIGH AND リターン格差 < RETURN_GAP_THRESHOLD AND ベータ差 < BETA_DIFF_THRESHOLD AND 追跡誤差 < TE_THRESHOLD
   ※市場代理フォールバック3の場合: ベータ差条件を除外し3条件で判定
3. **「不安定相関」**: 相関 > 0.7 AND 相関安定性 = 不安定
4. **「高相関（要確認）」**: 相関 > 0.7（上記いずれにも非該当）
5. **「低相関（分散効果）」**: 相関 < CORR_LOW

#### 出力形式

```
市場代理: {コード}（フォールバック: {1/2/3}）
算出期間: Nヶ月（信頼度: 高≥12 / 中6-11 / 低<6）

| 銘柄1 | 銘柄2 | 相関 | リターン格差 | ベータ差 | 年率TE | 分類 |
```

- 出力形式は `phase1-quant-analyst.md` の項目Bに準拠

### 3. 最大ドローダウン

- `00_portfolio_data.json` の `valuation_history` から累積最大値を計算
- ピーク日・ボトム日・最大ドローダウン率・回復期間を算出
- 出力形式は `phase1-quant-analyst.md` の項目Cに準拠

### 4. ストレスシナリオ基礎数値

- 基礎シナリオ（米国テック-20%、全世界株式-15%、円高進行）の影響額を計算
- 各シナリオについて: 対象銘柄・評価額変化・ポートフォリオ影響率を算出
- **追加シナリオの提案はPhase 1のペルソナエージェントが行う**（ここでは基礎シナリオの数値計算のみ）
- Phase 0.5では発生確率の判定は行わない（Phase 1のペルソナエージェントが判定する）
- 各シナリオに以下の追加フィールドを出力すること:
  - `absolute_loss`: 総資産 × 影響率（円単位）
  - `monthly_equivalent`: absolute_loss ÷ (total_asset ÷ 12)の絶対値（月数換算）
    - 表現: 「資産の約Xヶ月分に相当する減少」
    - **注意**: 「生活費Xヶ月分」ではない。total_asset÷12は「月当たり資産額」
- ただし、`0a_market_environment.md` の「分析への示唆」から基礎シナリオの発生条件に関連する記述があれば、テーブル下に「市場環境メモ」として箇条書きで転記する（判定はしない）
- 出力形式は `phase1-quant-analyst.md` の項目Dのテーブル形式に準拠（発生確率・市場環境根拠の列は空欄とする）

### 5. 加重平均スコア表

2つのテーブルを出力する:

**保有比率の算出（共通）**:
- `holdings` の各銘柄の `current_value` からポートフォリオ総額を算出
- 保有比率 = `current_value / 総額`（**小数値 0.0〜1.0** で扱う。例: 32.2% → 0.322）
- **絶対禁止**: パーセンテージ値（32.2等）をそのまま掛け算しないこと。必ず100で割って小数に変換する

**テーブル1: 5軸加重平均**（視点に依存しない）
- `00_portfolio_data.json` の `score_cache` と保有比率から、5軸（配当力・コスト効率・規模信頼性・売買品質・リターン）の加重平均を計算
- 計算式: `Σ(銘柄iの軸Xスコア × 銘柄iの保有比率)` ※保有比率は小数（0-1）、スコアは0-100点
- **5軸すべてのスコアが揃っていることを確認する**。score_cacheの各銘柄レコードから5軸（dividend_power, cost_efficiency, scale_reliability, trading_quality, return_performance）を取得し、欠落している軸がある場合はスキップせず、score_cacheの別のperspectiveレコードや`_metadata.score_cache.axes`を参照して取得する（5軸スコアは視点によらず同一値のため）
- 最弱軸・最強軸を特定し、差分を算出

**テーブル2: 視点別総合スコア加重平均**（視点ごとに異なる）
- `score_cache` の各銘柄の `total_score` を視点別に保有比率で加重平均
- 計算式: `Σ(銘柄iのtotal_score(視点Y) × 銘柄iの保有比率)` ※保有比率は小数（0-1）
- 6視点（balance, dividend, growth, low-cost, stability, volume）それぞれの値を出力

**バリデーション（必須）**:
- 計算結果が **0〜100の範囲** に収まることを確認する
- 範囲外の場合は保有比率のスケール誤り（パーセンテージと小数の混同）を疑い、計算を見直す

- 出力形式は `phase1-score-analyst.md` の項目Aに準拠

### 6. モメンタム分布

- `00_portfolio_data.json` の `etf_data` から各銘柄の `momentum_label` を取得
- ラベル別の銘柄数・保有比率を集計
- 下降中/下降加速の銘柄リストを出力
- 出力形式は `phase1-score-analyst.md` の項目Bの分布部分に準拠（要因分析・判定はペルソナ側で行う）

### 7. VaR/CVaR分析

- 相関分析（項目2）で算出した月次リターンデータを再利用
- 各銘柄の保有比率（**小数 0-1**。項目5と同じ算出方法）で加重し、ポートフォリオ月次リターンを算出
- VaR(95%) = ポートフォリオ月次リターンの5パーセンタイル
- CVaR(95%) = VaR以下の月次リターンの平均値
- 想定損失額 = VaR（またはCVaR）× ポートフォリオ総評価額
- 出力形式は `phase1-quant-analyst.md` の項目Fに準拠
- 各計算結果セクションの冒頭に `<!-- DO_NOT_RECALCULATE: var_cvar -->` マーカーを付与する

**スキップ条件（5段階）**:
- 月次リターンデータが6ヶ月未満: スキップ（「データ不足のためスキップ」と記載）
- 6-11ヶ月: 算出するが「参考値（データ点数: N点、信頼度: 低。N点中下位1点のみが5%タイルに該当するため、VaRとCVaRが同値になります。テールリスクの推定精度は極めて限定的です）」と注記
- 12-23ヶ月: 算出（「データ点数: N点、信頼度: 中（N点中下位M点が5%タイルに該当。サンプル数の増加に伴い推定精度は改善しますが、依然として参考値です）」と注記。Mは ceil(N*0.05) で算出）
- 24-35ヶ月: 算出（「データ点数: N点、信頼度: 中（テールリスク推定には依然として不十分。参考値として利用してください）」と注記）
- 36ヶ月以上: 算出（「データ点数: N点、信頼度: 高」と注記）

---

## フォールバック

`00_portfolio_data.json` の特定データが欠落している場合（`_data_status` のstatusが "error" または "empty"、あるいは必要なフィールドが存在しない場合）:

- その項目のセクションに「**データ不足のためスキップ**（理由: [具体的な欠落データ]）」と記載する
- Phase 1のペルソナエージェントが従来通り自身で計算・判断できるよう、スキップ理由を明確にする

---

## 計算メタデータ（出力冒頭に必須）

`05_shared_calculations.md` の冒頭（項目1の前）に、以下のメタデータセクションを出力すること。後続Phase で前提条件を確認する際の基準情報となる。

```markdown
## 計算メタデータ

| 項目 | 値 |
|------|-----|
| 分析実行日 | YYYY-MM-DD |
| リスクフリーレート | X.XX%（出典: 財務省国債金利情報 YYYY-MM-DD時点 / フォールバック値 0.5%） |
| 市場代理ETF | XXXX（選択理由: ...） |
| 月次リターンデータ点数 | XX点（YYYY-MM〜YYYY-MM） |
| 日次データ点数（30日） | XX点 |
| 日次データ点数（250日） | XX点 |
```

- 各値は実際の計算で使用した値を記入する
- リスクフリーレートはフォールバック値を使用した場合、その旨を明記する
- 日次データが未取得の場合は「未取得」と記載する

---

## 出力形式ルール

- 各項目はMarkdownのセクション（`##`）で区切る
- テーブル形式で数値を整理する
- **解釈や判断は一切含めない**（純粋な計算結果のみ）
- 例: 「シャープレシオが低いため見直し推奨」のような判定文は記載しない
- 例: 「高相関ペアがリスク集中を示唆」のような解釈も記載しない
- 各計算結果セクションの冒頭に項目別識別子付きの `<!-- DO_NOT_RECALCULATE: {識別子} -->` マーカーを付与する
  - 項目1: `<!-- DO_NOT_RECALCULATE: sharpe_ratio -->`
  - 項目2: `<!-- DO_NOT_RECALCULATE: correlation_v2 -->`
  - 項目3: `<!-- DO_NOT_RECALCULATE: max_drawdown -->`
  - 項目4: `<!-- DO_NOT_RECALCULATE: stress_scenario -->`
  - 項目5: `<!-- DO_NOT_RECALCULATE: weighted_score -->`
  - 項目6: `<!-- DO_NOT_RECALCULATE: momentum -->`
  - 項目7: `<!-- DO_NOT_RECALCULATE: var_cvar -->`
- Phase 1のペルソナエージェントは、このマーカーがある項目の数値を変更してはならない
- フォーマット例:
  ```
  ## 1. シャープレシオ分析・リスク調整後ランキング
  <!-- DO_NOT_RECALCULATE: sharpe_ratio -->
  使用リスクフリーレート: 1.32%（日本国債10年利回り）

  | 銘柄コード | 銘柄名 | 1年リターン | ボラティリティ | シャープレシオ | ランク |
  ```

---

## ペルソナ別活用パラメータの基礎数値（analyst-D/E向け共通計算）

以下の基礎数値を `05_shared_calculations.md` 内の末尾セクションに追加出力する。解釈はPhase 1のペルソナエージェントが独自に行う。

### analyst-D（マクロ戦略派）向け: マクロストレスシナリオ基礎数値

項目4の基礎シナリオに加え、以下のマクロシナリオの数値を計算して出力する:

| マクロシナリオ | 仮定 | 計算対象 |
|------------|------|--------|
| 金利急騰シナリオ | 金利+2%（全銘柄のデュレーション感応度を0.5として概算） | 各銘柄の評価額変化額・ポートフォリオ影響率 |
| 為替急変シナリオ（円高） | 円高+20%（外国資産構成比率を用いて概算） | 各銘柄の評価額変化額・ポートフォリオ影響率 |
| 為替急変シナリオ（円安） | 円安-20%（外国資産構成比率を用いて概算） | 各銘柄の評価額変化額・ポートフォリオ影響率 |

- 各銘柄の外国資産比率が `etf_data` のタグやカテゴリから推定可能な場合は推定値を使用。不明の場合は「外国資産比率: 不明（平均値で代替）」と注記する
- 発生確率・市場環境解釈はanalyst-Dが独自に判定する（ここでは数値のみ）
- 出力識別子: `<!-- DO_NOT_RECALCULATE: macro_stress -->`

### analyst-E（長期構造派）向け: 信託報酬の長期複利効果テーブル

保有銘柄の信託報酬差異を長期複利効果で数値化した基礎テーブルを出力する:

- `etf_data` の各銘柄の信託報酬（expense_ratio）を取得
- 最低コスト銘柄を基準として、各銘柄との信託報酬差分を計算
- 差分が長期複利で生む累積損失（概算）をテーブル化:

| 銘柄コード | 銘柄名 | 信託報酬 | 最低コストとの差分 | 5年複利影響 | 10年複利影響 | 20年複利影響 |
|---------|------|---------|----------------|-----------|------------|------------|
| ...     | ...  | 0.XX%   | +0.YY%         | -YY%      | -ZZ%       | -WW%       |

- 計算式: `(1 - 差分/100)^N - 1`（Nは期間年数）
- **解釈（どのETFのコストが問題か等）はanalyst-Eが独自に判断する**
- このセクションには数値テーブルのみを出力し、判定文・推奨文は記載しない
- 信託報酬データが取得できない銘柄は「信託報酬: 不明」と記載してスキップ
- 出力識別子: `<!-- DO_NOT_RECALCULATE: expense_ratio_effect -->`

---

## 追加計算（テクニカル指標）: 項目8-13

Phase 0で取得した追加データ（`price_data_daily_30d`, `price_data_close_250d`, `dividend_data`）を使用し、トレーダー知見に基づくテクニカル指標を計算する。

**計算指示の参照先**: `{skill_dir}/agent-instructions/phase05b-trader-indicators.md`

上記ファイルに記載された項目8-13の計算を実行し、結果を `05_shared_calculations.md` の末尾に追記する。各計算結果には `DO_NOT_RECALCULATE` マーカーを付与すること。

### 条件分岐

- **項目8-12**: Phase 0の追加データ（`price_data_daily_30d`, `price_data_close_250d`, `dividend_data`）が取得済みであれば実行する。個別のスキップ条件は `phase05b-trader-indicators.md` を参照
- **項目13（経済象限判定）**: `{WORK_DIR}/0a_market_environment.md` が存在する場合のみ実行する。Phase 0a完了を待たずにPhase 0.5は起動可能だが、0aが完了している場合のみ経済象限判定を計算する。未完了の場合は「Phase 0a未完了のためスキップ」と記載

### タイムアウト

追加計算の分、Phase 0.5のタイムアウトを **3分 → 5分** に変更する。

---

### [G] SR文脈テーブル（SELL推奨時の定量根拠義務化）※ debateモード専用

**実行条件**:
- 保有銘柄数 ≥ 2（相関ペアが1組以上存在する場合のみ実行）
- 1銘柄のみ保有の場合はセクションGをスキップし「相関ペア不足のためスキップ」と記載

**計算手順**:
1. 相関分析（セクションC）の結果から、各銘柄の「最高相関対銘柄」を特定
   - 相関ペアが存在しない銘柄は「相関ペアなし」として実効リターン差列を「-」と記載
2. 実効リターン差を計算:
   `実効リターン差 = (対象銘柄1年リターン - 信託報酬) - (最高相関対銘柄1年リターン - 対銘柄信託報酬)`
   - 信託報酬データが不明な銘柄は「信託報酬不明のため算出不可」と記載
3. SRが算出可能な銘柄の上位3銘柄をSR文脈テーブルとして出力
   - SR算出不可の銘柄（データ不足等）は「SR: 算出不可」として除外し、その旨を注記する
   - 銘柄数が3未満の場合は全銘柄を出力

**出力フォーマット**:
| コード | SR | SRランク | 最高相関対銘柄（相関係数） | 実効リターン差 | SELL推奨時必須根拠 |
|-------|-----|---------|----------------------|------------|-----------------|
| XXXX | X.XX | X位 | XXXX（X.XXX） | +XX.X% | SR低下 or ATR発動 or モメンタム失速 |

**ルール**: SRランク上位3位以内の銘柄をSELL推奨する場合は「SELL推奨時必須根拠」のいずれか1つ以上を定量的に確認・記載すること。コスト・過熱・マクロ逆風のみを根拠とするSELL推奨は「条件付きSELL（○○発動時）」として記載する。
