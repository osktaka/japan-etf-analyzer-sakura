# メール通知仕様

## 概要

Daily Advisor は ETF ポートフォリオの運用判断を支援するメール通知を 3 種類（+ 緊急アラート 1 種）送信する。
本ドキュメントは件名・本文の仕様、モバイル最適化ルール、用語翻訳、テスト戦略を一元管理する。
バッチスケジュール・依存関係は [`10_バッチ処理設計.md`](10_バッチ処理設計.md) を参照。

### Daily Advisor 3 種メール（+ alert）

| kind      | 配信タイミング       | 目的                                                |
| --------- | -------------------- | --------------------------------------------------- |
| morning   | 平日 07:00           | 当日の発注（売却・買付）通知＋採用銘柄配分（8銘柄+現金10%）・採用外保有・次回リバランス基準日カウントダウン・四半期末売買アクションを統合 |
| evening   | 平日 17:30           | 終値ベースの当日変動と配分（A群/B群/現金）・ルール状態を振り返る    |
| weekly    | 金曜 18:00           | 5 営業日のα・ベンチマーク比較と来週の推奨アクション |
| alert     | 9:00–15:00 平日 5 分間隔 | 機械ルール（損切・利確・N225急落・配分逸脱）即時通知（緊急アラート） |

**2026-05-14 改修**: 旧 `rebalance` kind（07:15配信）を廃止し、morning kind に統合した。
リバランス計画（採用銘柄配分テーブル・採用外保有・四半期末リバランス実行案内）は morning 本文に組み込まれる。

### 文体方針: アナリスト視点

「機械的な数値羅列」ではなく「結論先出し → 文脈 → 根拠」のアナリストレポート文体で記述する。
リード文 `summary` は以下の 3 層構造を維持する。

1. **結論**: 今日（本日／今週）何をすべきか／通常運用継続か
2. **文脈**: 前日比・月初比・α・配分状態などの数値
3. **根拠**: critical 発動の戦略書条項 or 配分逸脱の警告閾値超過 pp

静観時も「特になし。通常運用継続」を明示し、ノーニュースもニュースとして扱う。

## 件名フォーマット

全件名は `[M/D 区分] 結論` 形式。`SUBJECT_MAX_LEN = 40` 文字を目安にし、
超過時は `notification_renderer` から WARNING ログを出す。
`user_id` は件名に含めない。

| kind      | 静観時                       | アクション/警告時                                | 例外（複数件数時）                  |
| --------- | ---------------------------- | ------------------------------------------------ | ----------------------------------- |
| morning   | `[4/30 朝] 通常運用日`       | `[4/30 朝] アクション2件・所要5分`               | `[4/30 朝] 配分逸脱2件` / `[6/30 朝] リバランス実行日／売3件・買5件` |
| evening   | `[4/29 夕] 本日 -0.3%`       | `[4/29 夕] 本日 -1.2% / 配分逸脱2件`             | —                                   |
| weekly    | `[5/1 週次] α -1.0pp`        | `[5/1 週次] α -3.0pp / 来週1件`                  | —                                   |
| alert     | —（trigger 0 件は通常配送なし） | `[4/29 緊急] 損切到達: 1306` / `[4/29 要確認] N225急落` | `[4/29 緊急] 機械ルール3件発動` |

実装は `backend/src/services/notification_renderer.py` の `subject_for()` および
`_subject_morning / _subject_evening / _subject_weekly / _subject_alert` を参照。
件名が `SUBJECT_MAX_LEN`（40 文字）を超えた場合は WARNING ログを出し、末尾を「…」に切り詰める。

## 本文構造（4 種共通）

すべて「結論 → 文脈 → 根拠」の 3 層を踏襲する。

| kind      | セクション順序                                                                                                       |
| --------- | -------------------------------------------------------------------------------------------------------------------- |
| morning   | 結論ボックス → 文脈（前日比・月初比・現金比率・個別逸脱件数）→ **採用銘柄配分テーブル（8銘柄＋現金10%）** → **採用外保有（次回リバランスで全量売却推奨）** → **四半期末日のリバランス実行案内**（売却/買付） → 機械ルール → 戦略書フッター |
| evening   | 結論ボックス → 本日の市場文脈 → 配分（A群/B群/現金 3行、`is_warn` バッジ）→ 採用外保有（あれば） → 機械ルール → 戦略書フッター |
| weekly    | 結論ボックス（α + 配分逸脱件数）→ 5 営業日サマリ → 配分カテゴリ別（A群/B群/現金） → 採用外保有（あれば） → 来週の推奨アクション → 今週の機械ルール → 戦略書フッター |
| alert     | severity 別カラー結論ボックス（赤=critical / 黄=warn / 青=info）→ 各 trigger（推奨アクション・メッセージ・payload 縦並び）→ 戦略書フッター |

### morning kind のリバランス統合仕様（2026-05-14 統合）

旧 `rebalance` kind（07:15）の機能を morning kind 本文に統合した。

- **目標配分**: A群（2559=15% / 1540=15% / 200A=15%）+ B群（1306=9.00% / 1629=9.00% / 1615=9.00% / 2646=9.00% / 1618=9.00%）+ 現金=10%。合計 100.00%（SSOT: `docs/12_personal_strategy.md`）
- **次回リバランス基準日**: 3/6/9/12 月の最終営業日（`jpholiday` で土日・祝日を除外）。当日が基準日ならその日を返す
- **逸脱閾値**: `OK`（< ±`drift_ok_pp`pp） / `WARN`（±`drift_ok_pp` 〜 `drift_warn_pp`pp） / `CRITICAL`（≥ ±`drift_warn_pp`pp）。閾値は `mechanical_rules.drift_ok_pp` / `mechanical_rules.drift_warn_pp` から取得（既定 3.0 / 5.0）。採用外保有銘柄は常に `CRITICAL` 扱い
- **アクション算出**: `floor((目標金額 - 現在評価額) / 当日close)`。単元未満は次回繰越。採用外は全量売却
- **結論ボックスの色分け**: 通常日は青系（`#f7f9fc`）、四半期末リバランス基準日は黄系（`#fff8e1`）で「本日実行」を強調
- 実装: `backend/src/services/portfolio_rebalance_service.py:calculate_rebalance_plan()`（strategy DI 駆動）
- バッチ: `backend/scripts/daily_advisor_morning.py`（07:00 JST 平日、`CRON_BATCH_PROFILE=dev` 限定）

Markdown 版はスタイル不適用のため文体のみ統一し、装飾はしない（モバイル対応対象外）。

## モバイル対応 CSS ルール（HTML のみ）

Gmail iOS / Gmail Web / Apple Mail を主要ターゲットとする。
ダークモード対応は Gmail で実質無視されるためスキップする。

### 必須ルール

- `<meta name="viewport" content="width=device-width, initial-scale=1.0">` 設定必須
- `<meta name="x-apple-disable-message-reformatting">` で iOS 自動リフロー抑止
- レイアウトは 1 カラム、`<table>` 禁止
- `<div style="max-width:600px;margin:0 auto;padding:16px;">` を root コンテナに使用
- 「ラベル + 値」の並びは `<dl><dt><dd>` または `<p>` で縦並び
- フォントは最低 14px（推奨 16px）、`line-height:1.6`
- 配色固定: `background:#fff` / `color:#222`
- リンクのタップ領域は `padding:12px 16px;display:inline-block;`（44px 相当）
- CSS は全インライン、外部 CSS / `<font>` タグ禁止

### 結論ボックス

- 通常運用: `background:#f7f9fc;border-left:4px solid #2c5aa0;`
- alert critical: 赤系 `#fdecec / #c0392b`
- alert warn: 黄系 `#fff8e1 / #c79100`
- alert info: 青系 `#e7f1fb / #2c5aa0`

詳細スタイルは `backend/src/services/templates/advisor/*.html.j2` のインライン CSS を参照。

## 用語翻訳表

`backend/src/services/templates/advisor/_macros.j2` で集中管理。テンプレートは必ず `{% from '_macros.j2' import bucket_label, rule_kind_label, action_label %}` で取り込む。

### bucket（アロケーションのカテゴリ）

| 内部値    | 表示                  |
| --------- | --------------------- |
| `group_a` | A群（コア・逆相関）   |
| `group_b` | B群（日本株テーマ）   |
| `cash`    | 現金                  |
| `other`   | 採用外                |

> 2026-05-14 改修: 旧 `core` / `theme` を廃止し、A群（コア・逆相関）/B群（日本株テーマ）モデルに統一。`other` は採用外保有（次回リバランスで売却推奨）を示す。

### rule_kind（機械ルール種別）

| 内部値              | 表示                       |
| ------------------- | -------------------------- |
| `loss_cut`          | 損切りライン到達           |
| `take_profit_1`     | 利確ライン到達（第 1 段）  |
| `take_profit_2`     | 利確ライン到達（第 2 段）  |
| `n225_drawdown`     | N225 急落                  |
| `allocation_drift`  | 配分逸脱                   |

### action（売却/買付アクション）

| 内部値                    | 表示                |
| ------------------------- | ------------------- |
| `all`                     | 全量売却            |
| `schedule`                | 月次定期売却        |
| `dca` / `DCA`             | 定期積立（DCA）     |

## データコンテキスト（`NotificationContext`）

実装: `backend/src/services/daily_advisor_models.py:NotificationContext`。`@dataclass(frozen=True)` で不変。

| フィールド                | 型                          | 説明                                       |
| ------------------------- | --------------------------- | ------------------------------------------ |
| `kind`                    | `str`                       | `"morning" / "evening" / "weekly" / "alert"` |
| `today`                   | `date`                      | 通知基準日（JST）                          |
| `user_id`                 | `str`                       | 受信者識別。**件名には載せない**           |
| `strategy_revision`       | `date`                      | 戦略書 revision 日付（フッターに表示）     |
| `benchmark`               | `str`                       | ベンチマーク（既定 `^N225`）               |
| `drift_ok_pp`             | `float`                     | 配分逸脱の警告閾値（OK境界、SSOT: `mechanical_rules.drift_ok_pp`、必須） |
| `drift_warn_pp`           | `float`                     | 配分逸脱の警告閾値（WARN境界、SSOT: `mechanical_rules.drift_warn_pp`、必須） |
| `total_asset`             | `float`                     | 総資産（現金 + 評価額）                    |
| `total_value`             | `float`                     | 評価額                                     |
| `cash_balance`            | `float`                     | 現金残高                                   |
| `daily_change_pct`        | `Optional[float]`           | 前日比（%）                                |
| `holdings_count`          | `int`                       | 保有銘柄数                                 |
| `allocation_drifts`       | `Tuple[AllocationDrift, ...]` | 配分カテゴリ別の乖離                     |
| `triggers`                | `Tuple[RuleTrigger, ...]`   | 機械ルール発動                             |
| `alpha_pp`                | `Optional[float]`           | α（pp、weekly 専用）                       |
| `period_label`            | `Optional[str]`             | 期間ラベル（例: `過去5営業日`）            |
| `portfolio_return_pct`    | `Optional[float]`           | ポートフォリオリターン（%）                |
| `benchmark_return_pct`    | `Optional[float]`           | ベンチマークリターン（%）                  |
| `month_start_total_asset` | `Optional[float]`           | 月初時点総資産（None 時はテンプレで分岐）   |
| `month_start_change_pct`  | `Optional[float]`           | 月初比（%、None フォールバック）           |
| `extra`                   | `Dict[str, Any]`            | 拡張用                                     |

### 月初データの None フォールバック

`PortfolioService` に「月初時点総資産取得 API」が未実装のため、`month_start_*` は `None` で先行リリース。
テンプレートは `{% if month_start_change_pct is not none %}` で分岐し、
未取得時は当該行をスキップする。

### 派生フィールド（テンプレート渡し用）

`notification_renderer._context_to_params()` が以下も合わせて渡す。

| キー                | 算出元                              |
| ------------------- | ----------------------------------- |
| `urgency_tag`       | `urgency_tag(ctx)`（緊急/要対応/要確認/情報/静観） |
| `summary`           | `summary_for(ctx)`（3 行リード文）  |
| `action_count`      | morning kind は `rebalance_plan` の本日実行件数（`is_rebalance_day=True` 時のみ非ゼロ）、それ以外は 0 |
| `estimated_minutes` | `clamp(round(2 + 2.5 * action_count), 3, 30)` |

## テスト戦略

### 既存ユニットテスト

`backend/tests/unit/services/test_notification_renderer.py`

`subject_for / urgency_tag / summary_for / recommended_action / render` の挙動を 33 ケースで検証。
件名は文字列の構造（先頭が `[M/D 区分]` で始まる、`通常運用日` を含む等）を assert する設計とし、
将来のリライトに過度に脆くしない。

### スナップショットテスト

`backend/tests/unit/services/test_notification_renderer_snapshot.py`

9 ケース × md/html を `__snapshots__/` 配下のゴールデンファイルと完全一致比較する。

#### ケース一覧

| kind    | tag                  | 状況                              |
| ------- | -------------------- | --------------------------------- |
| morning | `seikan`             | 売買・トリガーなし                |
| morning | `critical`           | loss_cut critical 発動            |
| evening | `seikan`             | warn なし                         |
| evening | `warn_drift`         | 配分逸脱 warn                     |
| weekly  | `seikan`             | α=-1.0（>-2.0、静観）             |
| weekly  | `warn_alpha`         | α=-3.0（≦-2.0、要確認）           |
| alert   | `critical`           | loss_cut critical                 |
| alert   | `warn`               | n225_drawdown warn                |
| alert   | `info`               | n225_drawdown info                |

#### 期待値の更新手順

```bash
# 期待値をリライト後の出力で上書き
docker compose exec -T -e SNAPSHOT_UPDATE=1 backend \
    pytest tests/unit/services/test_notification_renderer_snapshot.py -v

# 差分確認後にコミット
git diff backend/tests/unit/services/__snapshots__/
```

外部ライブラリ（syrupy 等）は不採用＝YAGNI。`SNAPSHOT_UPDATE` 環境変数による
シンプルな手書き比較で運用する。

### 開発用 artifact ダンプ

`backend/scripts/_dev/dump_email_artifacts.py`

リライト時の差分目視用。`tmp/email_artifacts_before/` または
`tmp/email_artifacts_after/` に 9 ケース × md/html を出力する。
本テストではないため `pytest` 配下には置かない。

## 関連ファイル

| 種別           | ファイル                                                                  |
| -------------- | ------------------------------------------------------------------------- |
| データモデル   | `backend/src/services/daily_advisor_models.py`                            |
| context builder | `backend/src/services/daily_advisor_contexts.py`                          |
| renderer       | `backend/src/services/notification_renderer.py`                           |
| テンプレート   | `backend/src/services/templates/advisor/{morning,evening,weekly,alert}.{md,html}.j2` |
| 用語翻訳マクロ | `backend/src/services/templates/advisor/_macros.j2`                       |
| ユニットテスト | `backend/tests/unit/services/test_notification_renderer.py`               |
| スナップショット | `backend/tests/unit/services/test_notification_renderer_snapshot.py` ＋ `__snapshots__/` |
| dev ダンプ     | `backend/scripts/_dev/dump_email_artifacts.py`                            |

## デモユーザー向けポートフォリオ変更通知

Daily Advisor 系（morning/evening/weekly/alert）とは独立した、**demo ユーザーの自動取引バッチ専用**のレポートメール。
`backend/scripts/execute_demo_trades.py`（cron Step 7）が `POST /api/v1/demo/trades` を完了した直後に送信する。
詳細なバッチ仕様は [`10_バッチ処理設計.md`](10_バッチ処理設計.md) 6.5節を参照。

### 概要

| 項目 | 内容 |
| ---- | ---- |
| 目的 | demo ユーザーの自動取引前後のポートフォリオ状態と当日の POST 結果を1通で通知する |
| 送信タイミング | `execute_demo_trades.py` 末尾（batch_logs 更新・マーカー作成の後） |
| 宛先 | `NOTIFY_TO_EMAIL`（Daily Advisor 系と共有） |
| 実装 | `backend/src/services/demo_portfolio_notifier.py:notify()` |
| テンプレート | `backend/src/services/templates/demo/portfolio_change.{md,html}.j2` |

### 件名フォーマット

`SUBJECT_MAX_LEN = 40` 文字目安。`user_id` は固定で `demo` のため省略しない（advisor 系との識別子）。

| 状況 | 例 |
| ---- | ---- |
| 通常（成功のみ） | `[5/14 demo] 取引2件・資産¥1,015,200 (+2.3%)` |
| 部分失敗 | `[5/14 demo] 取引2/3件成功 ⚠️ 資産¥1,008K (+1.6%)` |
| 全件失敗 | `[5/14 demo] 取引3件全失敗 ❌` |
| dry-run（trigger=always 時） | `[5/14 demo/dry-run] 提案3件 (POST未実行)` |

ETF 銘柄名・コードは件名に載せず、集計値のみで構成する（複数銘柄取引時の桁あふれ対策）。

### 本文構造（7セクション）

1. **結論ボックス**: 取引件数（succeeded/failed）・資産合計・前日比（HTMLは `#f7f9fc` 系の薄青ボックス）
2. **本日のPOST取引一覧**: ETFコード / 銘柄名 / 売買 / 数量 / 参考価格 / 約定金額 / 結果 / memo
3. **ポートフォリオ概況（取引後）**: 現金残高 / 保有銘柄数 / 評価額 / 資産合計 / 前日比
4. **保有銘柄スナップショット（構成比降順）**: etf_code / 銘柄名 / 数量 / 現在値 / 評価額 / 含み損益 / 構成比%
5. **過去1週間の資産推移**: `/demo/portfolio/valuation-history` の末尾7日
6. **失敗詳細**: `failed > 0` の場合のみ表示（エラーメッセージ・該当 trade 情報）
7. **フッタ**: ログパス（`logs/execute_demo_trades.log`）・`batch_logs.id`・関連レポート MD への誘導

### 送信トリガー

2段階の環境変数でフェイルセーフを構成する。

| 環境変数 | 値 | 動作 |
| -------- | -- | ---- |
| `DEMO_PORTFOLIO_REPORT_ENABLED` | `0`（既定） | trigger に関わらず送信しない（1段目フェイルセーフ） |
| `DEMO_PORTFOLIO_REPORT_ENABLED` | `1` | trigger の判定に従って送信判断する |
| `DEMO_PORTFOLIO_REPORT_TRIGGER` | `on_trade`（既定） | `effective_execute=True` かつ `succeeded + failed > 0` の場合のみ送信 |
| `DEMO_PORTFOLIO_REPORT_TRIGGER` | `always` | dry-run でも送信（テンプレ動作確認・件名検証用） |
| `DEMO_PORTFOLIO_REPORT_TRIGGER` | `never` | 送信しない |

SMTP 例外は `demo_portfolio_notifier.notify()` 内部で捕捉し、戻り値（bool）にのみ反映する。
`execute_demo_trades.py` の exit code は通知失敗に影響されない（WARN ログのみ残す）。

### モバイル最適化方針

Daily Advisor 系（[モバイル対応 CSS ルール](#モバイル対応-css-ルールhtml-のみ)）と同じ方針を踏襲。

- `<meta name="viewport" content="width=device-width, initial-scale=1.0">` 設定必須
- `<div style="max-width:600px;margin:0 auto;padding:16px;">` で 1 カラム
- CSS は全インライン、外部 CSS / `<font>` タグ禁止
- 取引一覧テーブルは縦並びカード化（モバイル幅で横スクロール回避）
- フォント最低 14px・`line-height:1.6`・配色固定（`background:#fff` / `color:#222`）

### advisor 系との分離方針

advisor 系（morning/evening/weekly/alert）に手を入れずに併走できるよう、以下を完全に独立させる。

| 区分 | advisor 系 | demo ポートフォリオ通知 |
| ---- | ---------- | --------------------- |
| テンプレート | `templates/advisor/*.{md,html}.j2` | `templates/demo/portfolio_change.{md,html}.j2` |
| 件名生成 | `notification_renderer.subject_for()` | `demo_portfolio_notifier._build_subject()` |
| 環境変数 | `ADVISOR_USER_ID` 等 | `DEMO_PORTFOLIO_REPORT_ENABLED` / `DEMO_PORTFOLIO_REPORT_TRIGGER` |
| 件名プレフィックス | `[M/D 朝]` / `[M/D 夕]` / `[M/D 週次]` / `[M/D 緊急]` | `[M/D demo]` / `[M/D demo/dry-run]` |
| docs 章 | 本ドキュメント上部（morning/evening/weekly/alert） | 本章（独立） |
| テスト | `test_notification_renderer{,_snapshot}.py` | `test_demo_portfolio_notifier.py` |

### 関連ファイル

| 種別 | ファイル |
| ---- | -------- |
| 通知本体 | `backend/src/services/demo_portfolio_notifier.py` |
| テンプレ（md/html） | `backend/src/services/templates/demo/portfolio_change.{md,html}.j2` |
| 呼び出し元 | `backend/scripts/execute_demo_trades.py`（main 末尾） |
| ユニットテスト | `backend/tests/services/test_demo_portfolio_notifier.py`（18 ケース） |
| バッチ統合テスト | `backend/tests/test_execute_demo_trades.py`（通知関連 4 ケース追加） |

## 変更履歴

- **2026-05-14**: A群/B群モデル統一。旧 `core` / `theme` バケットを廃止し、`group_a`（A群・コア・逆相関）/ `group_b`（B群・日本株テーマ）/ `cash` / `other`（採用外）の 4 値に切り替え。`rebalance` kind（07:15配信）を廃止し、リバランス計画（採用銘柄配分テーブル・採用外保有・四半期末売買アクション・カウントダウン）を morning kind 本文に統合。配分閾値リテラル `5.0` / `3.0` をテンプレ変数 `drift_warn_pp` / `drift_ok_pp` に動的化（SSOT: `docs/12_personal_strategy.md` の `mechanical_rules`）。
- **2026-04-30**: 4 種テンプレート全面書き直し。アナリスト視点（結論 → 文脈 → 根拠）への文体統一、
  HTML のモバイル最適化（viewport / 1 カラム / 14px 以上 / インライン CSS）、
  件名フォーマットを `[M/D 区分] 結論` に短縮、`user_id` を件名から削除、
  用語翻訳マクロ `_macros.j2` 新設、`NotificationContext` に `month_start_*` を追加、
  スナップショットテスト 11 ケース新設。
