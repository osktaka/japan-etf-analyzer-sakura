# バッチ処理設計

## 1. 概要

ETFデータの自動更新・メンテナンスを行うバッチ処理群の設計。cronによるスケジュール実行と、障害時の自動リトライ機構を備える。

## 2. 共通基盤（BaseBatchScript）

すべてのバッチスクリプトは `backend/scripts/base_batch.py` の `BaseBatchScript` を継承する。

### 提供機能

| 機能 | 説明 | 設定 |
|------|------|------|
| Flask app context管理 | DB接続・モデル利用を自動化 | 常時有効 |
| バッチログ記録 | `batch_logs` テーブルに実行記録を保存 | `enable_batch_log` |
| 進捗追跡 | 処理済み件数・最終処理コードを記録 | `enable_progress` |
| レジューム機能 | `--resume BATCH_LOG_ID` で失敗箇所から再開 | `enable_resume` |
| 依存チェック | 依存バッチの完了を確認してから実行 | `depends_on` / `check_window` |
| dry-runモード | `--dry-run` で変更なしに実行内容を確認 | 常時有効 |

### クラス構成

- **`BaseBatchScript`** - フル機能の基底クラス（バッチログ・進捗・レジューム対応）
- **`SimpleBatchScript`** - バッチログなしの軽量版（batch_monitor用）

## 3. バッチスクリプト一覧

### 定期実行バッチ

| スクリプト | 説明 | データソース |
|-----------|------|-------------|
| `sync_etf_from_jpx.py` | JPXサイトからETF銘柄マスターを同期 | JPX |
| `update_etf_data.py` | 株価データ取得・パフォーマンスキャッシュ計算・勢いラベル更新 | Yahoo Finance |
| `sync_from_minkabu.py` | 配当利回り・純資産額をスクレイピングで取得 | minkabu.jp |
| `update_scores.py` | 6切り口のスコアキャッシュ更新・評価指標履歴保存（勢い履歴: momentum_label, 全8期間のregression_rate（回帰上昇率）および全8期間のreturn_rate（株価上昇率）（各1m/3m/6m/1y/3y/5y/10y/20y）をPerformanceCacheから取得してetf_metrics_historyに保存）。**営業日のみ実行（土日・祝日はスキップ）** | DB内部計算 |
| `sync_historical_splits.py` | 過去の株式分割データを一括同期 | Yahoo Finance |
| `rotate_logs.py` | ログファイルローテーション・古いログとバッチログの削除（BaseBatchScript継承） | ローカル |
| `batch_monitor.py` | タイムアウト検知・失敗バッチの自動リトライ | DB内部 |

### 手動実行スクリプト

| スクリプト | 説明 | 用途 |
|-----------|------|------|
| `seed_data.py` | 初期データ投入 | 初回セットアップ |
| `auto_tag_etfs.py` | ETFへのタグ自動付与 | タグルール変更時 |
| `create_admin.py` | 管理者ユーザー作成 | 初回セットアップ |
| `reset_user_password.py` | ユーザーパスワードリセット | 運用 |
| `fetch_etf_data.py` | 単一ETFのデータ取得（デバッグ用） | 調査 |
| `backfill_momentum_history.py` | 勢い履歴データのバックフィル（PriceHistoryから1m/3mのregression_rateを再計算してetf_metrics_historyに補完） | データ補完 |
| `backfill_momentum_history_extended.py` | 勢い履歴データの1年分拡張バックフィル（最適化版。ETFごとに一括PriceHistory読み込み、カットオフ日方式でマージ） | データ補完 |
| `backfill_return_rate_history.py` | 株価上昇率履歴データのバックフィル（PriceHistoryから1m/3mのreturn_rateを再計算してetf_metrics_historyに補完） | データ補完 |
| `recalc_momentum_labels.py` | performance_cacheの既存値からmomentum_labelのみ再計算（株価再取得・履歴書き込みなし） | データ修正 |
| `simulation/regression.py` | 回帰計算ユーティリティ（最小二乗法による直線回帰からリターン率を算出） | ライブラリ |
| `simulation/engine.py` | シミュレーションエンジン（シグナルベースの単一ポジション売買シミュレーター） | ライブラリ |
| `simulation/simulate.py` | モメンタム売買シミュレーション（勢いラベル/閾値の2戦略で仮想売買し損益を検証） | 分析・検証 |
| `simulation/optimize.py` | モメンタム戦略パラメータ最適化（グリッドサーチ、10銘柄） | 分析・検証 |
| `simulation/optimize_comprehensive.py` | モメンタム戦略包括的最適化（4パターン検証、50銘柄×4評価軸） | 分析・検証 |

## 4. スケジュール（cron-batch.sh ディスパッチャ）

タイムゾーン: Asia/Tokyo

開発環境では **ホストcron + `scripts/cron-batch.sh`** で全ジョブを集約管理する（schedulerコンテナは2026-04-29に廃止）。ホストcronは1エントリのみ:

```cron
*/5 * * * * /path/to/japan-etf-analyzer-sakura/scripts/cron-batch.sh >> /path/to/japan-etf-analyzer-sakura/logs/cron-batch.log 2>&1
```

スクリプト内部で時刻・曜日・祝日判定を行い、該当ジョブを `docker compose exec -T backend python3 scripts/<NAME>.py` で起動する。各バッチは `flock`（`/tmp/cron-batch-<NAME>.lock`）で多重起動防止、バックグラウンド並列実行（チェーン実行時のみ同期 fail-stop）。

祝日判定（`is_holiday`）は当日結果を `/tmp/etf_jpholiday_YYYYMMDD.cache` にキャッシュし `cron-etf-rating-daily.sh` と共有することで、`docker compose exec` の発行を1日1回に抑える（CPUスパイク対策）。

### 環境プロファイル

`CRON_BATCH_PROFILE=dev|prod`（デフォルト `dev`）で実行ジョブを切替える。不正値は `exit 2` で起動拒否。本番（さくら）デプロイ時は `prod` に設定し、開発専用ジョブ6本（advisor 3本＝morning/evening/weekly ＋theme_etfs＋mechanical_rule_watcher＋etf_rating_daily）を自動的に除外する。

### スケジュール表

| 時刻 | 曜日 | スクリプト | プロファイル | 備考 |
|------|------|-----------|------------|------|
| 06:00 | 月曜（祝日でも実行） | `sync_etf_from_jpx.py` → `update_scores.py --skip-dep-check` → `sync_historical_splits.py --all --rate-limit 3.0` | both | マスタ更新チェーン（fail-stop連結。途中失敗で後続スキップ） |
| 16:00 | 平日（祝日スキップ） | `update_etf_data.py --smart --rate-limit 3.0` | both | 市場閉場後に株価データ取得 |
| 16:00 | 平日（祝日スキップ） | `sync_from_minkabu.py --rate-limit 1.5` | both | 配当利回り・純資産額更新（独立実行） |
| `*/10 16-20` | 平日（祝日スキップ） | `update_scores.py` | both | 10分間隔で依存チェック付き実行 |
| 03:00 | 日曜 | `update_theme_etfs.py` | **dev** | テーマETF自動選定 |
| 05:00 | 毎日（祝日含む） | `rotate_logs.py` | both | ログローテーション |
| `*/5` | 毎日（祝日含む） | `batch_monitor.py` | both | 5分間隔で監視・自動リトライ |
| 07:00 | 平日（祝日スキップ） | `daily_advisor_morning.py` | **dev** | **寄り付き前のアクション提示**。米国 overnight 市況サマリ（S&P500/NASDAQ/DOW/USDJPY/VIX/日経先物 NKD=F）＋前夜決定事項リマインダー＋機械ルール＋次回リバランス基準日カウントダウン。銘柄別配分テーブル・売買プラン本体は evening に集約済み |
| 17:30 | 平日（祝日スキップ） | `daily_advisor_evening.py` | **dev** | **終値ベースの完全状態確認＋翌営業日の発注材料**。A群/B群/現金 3 行配分サマリ＋銘柄別目標配分テーブル（8銘柄＋現金）＋採用外保有詳細＋売却/買付プラン（`is_rebalance_day` で「本日リバランス実行」/「次回基準日へのプラン」を分岐）。`update_etf_data` 16:00 完了後に起動 |
| 18:00 | 金曜（祝日でも実行） | `daily_advisor_weekly.py` | **dev** | 週次レビュー通知（週末入りの整理目的） |
| 月-木 18:00 / 金 18:15 | 平日（祝日スキップ） | `etf_rating_daily`（`scripts/cron-etf-rating-daily.sh`） | **dev** | **個別銘柄の中期マッチ度採点**。target_holdings + watchlist 全銘柄を上昇10×下落10で採点、Top5重み×2、ネット中期スコア・3シナリオを1通メール配信。`update_etf_data` 16:00 / `sync_from_minkabu` 16:00 完了後の終値データに依存。金曜のみ 18:15 にずらすのは weekly 18:00 との並列回避。詳細評価フレームは [`13_銘柄評価設計.md`](13_銘柄評価設計.md)、メール仕様は [`06b_メール通知仕様.md`](06b_メール通知仕様.md) 参照 |
| `*/5 9-15` | 平日（祝日スキップ） | `mechanical_rule_watcher.py` | **dev** | 機械ルール監視（損切・N225急落・配分逸脱。東証取引時間帯。利確は2026-06-04廃止） |

※ メール本文（`daily_advisor_*` / `mechanical_rule_watcher` から送信される morning / evening / weekly / alert）の件名・本文構造・モバイル最適化ルール・用語翻訳・テスト戦略は [`06b_メール通知仕様.md`](06b_メール通知仕様.md) を参照。リバランス通知は 2026-05-14 に morning に統合済（rebalance kind 廃止）。

### 月曜06:00 マスタ更新チェーン

`run_chain` ヘルパーにより、3つのバッチを fail-stop 連結で同期実行する:

1. `sync_etf_from_jpx.py`（銘柄マスター更新）
2. `update_scores.py --skip-dep-check`（スコア再計算）
3. `sync_historical_splits.py --all --rate-limit 3.0`（分割履歴同期）

途中で失敗した場合、後続バッチはスキップされる。本番crontabの `&&` 連結と同等の挙動。

## 5. 依存関係

```
sync_etf_from_jpx.py ──→ update_scores.py
  (銘柄マスター更新)        (スコア再計算)

update_etf_data.py ──→ update_scores.py
  (株価取得)               (スコア再計算・勢い履歴保存)
  ├─ performance_cache 更新 ─→ regression_rate, return_rate (各全8期間) を参照
  └─ momentum_label 更新

sync_from_minkabu.py ──→ update_scores.py
  (配当利回り・純資産額更新)        (スコア再計算)

update_etf_data.py    ──┐
                        ├─→ etf_rating_daily（cron-etf-rating-daily.sh）
sync_from_minkabu.py  ──┘     (Claude CLI → /etf-rating all --send-mail → etf_rating_send_mail.py)
                              ※ 終値・配当利回りデータを参照するため両者の完了を待つ
                              ※ 月-木 18:00 / 金 18:15 起動なので、16:00 系の完了がほぼ保証される時間帯。
                              ※ 金曜のみ 18:15 にずらすのは daily_advisor_weekly (18:00) との並列を避けるため。
                                 cron-batch.sh の dispatch は時刻判定のみで、明示的な
                                 `depends_on` チェックは BaseBatchScript 経由ではない
                                 （Claude CLI 起動のため）。終値未取得時は Phase 0/1 が
                                 ChartService API 経由で取得失敗を検知して当該銘柄をスキップする
```

### 依存ルール

- `update_scores.py` は `performance_cache` テーブルの `return_rate`・`volatility` および `regression_rate`（全8期間: 1m, 3m, 6m, 1y, 3y, 5y, 10y, 20y）を参照するため、`update_etf_data.py` の後に実行する
- `update_scores.py` は配当利回り（`etfs.dividend_yield`）を参照するため、`sync_from_minkabu.py` の後に実行する
- `update_scores.py` は営業日判定を内蔵（`jpholiday`で祝日、`weekday()`で土日を判定）し、非営業日はバッチログを作成せずスキップする。これにより祝日に不要な `etf_metrics_history` レコードが作成されることを防止する

### 依存チェック機能

`BaseBatchScript` のクラス属性 `depends_on` と `check_window` により、依存バッチの完了を自動チェックする仕組み。従来のスケジュール順序（schedulerコンテナ内cron）による暗黙的な担保に代わり、明示的な依存チェックで実行順序を保証する。

#### クラス属性

| 属性 | 型 | 説明 |
|------|-----|------|
| `depends_on` | `list[str]` | 依存するバッチ名のリスト（例: `["update_etf_data", "sync_from_minkabu"]`） |
| `check_window` | `tuple[str, str]` | 依存チェックを行う時間帯（JST）。例: `("16:30", "22:00")` で16:30〜22:00の範囲外はスキップ |

#### チェックフロー

1. **時間帯判定**: 現在時刻（JST）が `check_window` の範囲外ならスキップ
2. **依存完了確認**: `depends_on` の全バッチが今日（JST）成功しているか確認。未完了ならスキップ（次のcron周期で再チェック）
3. **自バッチ未成功判定**: 自バッチが今日まだ成功していなければ実行（初回実行）
4. **依存バッチ時刻比較**: 依存バッチの最新成功時刻（`finished_at`）のいずれかが自バッチの最新成功時刻より新しければ実行（新データあり）。すべて自バッチより古ければスキップ（処理済み）

#### バイパス条件

以下の場合、依存チェックは自動的にスキップされる:

- **`--skip-dep-check`**: 依存チェックを明示的にスキップ。月曜cronのように `sync_etf_from_jpx.py` とチェーン実行する場合に使用（直前のバッチ完了が保証されているため）
- **`--resume`**: 失敗したバッチの途中再開時。依存バッチは前回実行時に既に完了しているため再チェック不要

## 6. ログ管理

### ログファイル

| ファイル | 出力元 |
|---------|--------|
| `logs/master_sync.log` | `sync_etf_from_jpx.py` |
| `logs/etf_update.log` | `update_etf_data.py` |
| `logs/minkabu_sync.log` | `sync_from_minkabu.py` |
| `logs/score_update.log` | `update_scores.py` |
| `logs/split_sync.log` | `sync_historical_splits.py` |
| `logs/theme_etfs.log` | `update_theme_etfs.py` |
| `logs/rotate.log` | `rotate_logs.py` |
| `logs/batch_monitor.log` | `batch_monitor.py` |
| `logs/advisor_morning.log` | `daily_advisor_morning.py` |
| `logs/advisor_evening.log` | `daily_advisor_evening.py` |
| `logs/advisor_weekly.log` | `daily_advisor_weekly.py` |
| `logs/advisor_watcher.log` | `mechanical_rule_watcher.py` |
| `logs/etf_rating.log` | `scripts/cron-etf-rating-daily.sh`（Claude CLI → `etf_rating_send_mail.py`） |
| `logs/cron-batch.log` | `scripts/cron-batch.sh`（ディスパッチャ自身の起動ログ） |

### ローテーションポリシー

- `rotate_logs.py` が毎日 05:00 に実行
- 10MB超のログファイルをローテーション（最大5世代保持）
- 90日超の古いログを自動削除
- DBの `batch_logs` レコードも90日超で削除

## 6.5 demo取引POST自動化（Step 7）

`scripts/cron-portfolio-analysis.sh` の **Step 7** として `backend/scripts/execute_demo_trades.py` を起動し、`/portfolio-analysis-v2` スキルが生成した取引計画JSONを `POST /api/v1/demo/trades` に自動投入する仕組み。

### 処理フロー

```
1. マーカー判定         reports/demo/.trades_posted_YYYYMMDD が存在 → exit 0（当日処理済み）
2. JSON読込             reports/demo/trades_execution_plan.json をロード（欠落時は exit 0、正常終了）
3. trades[] 空判定      空配列なら "no trades to execute" ログのみで exit 0（マーカー作成なし）
4. DB重複検査           plan_id 有 → memo の `[plan_id=...]` タグ＋etf_code/trade_type 一致
                       plan_id 無 → memo `[auto]` プレフィックス＋etf_code/trade_type 一致
5. POST                 dev: `http://localhost:8902` / prod: `https://kima3.net/japan-etf-analyzer`
6. 結果ログ             logs/execute_demo_trades.log ＋ stdout に成功/失敗を出力
7. batch_logs 記録      name=`demo_trade_post` でDB記録（status / total_count / error_message）
8. マーカー作成         実POST実行モードかつ全成功 or 部分成功時に `reports/demo/.trades_posted_YYYYMMDD` を作成
                       （dry-run および 全失敗時は作成しない＝再実行可能）
```

> **価格鮮度チェック**: 直近終値との乖離判定は `/portfolio-analysis-v2` スキル側で行い、確定値（`price`）として JSON に書き込む方針（`skipped[]` 配列に乖離超過分を記録）。`execute_demo_trades.py` 内では再フェッチ・乖離判定は行わない。

### Idempotency（二段構え）

| 層 | 機構 | 役割 |
|----|------|------|
| 1段目 | マーカーファイル | 当日2回目以降の起動を即時 short-circuit（POST処理自体をスキップ） |
| 2段目 | DB重複検査 | マーカーが破損・削除された場合のフェイルセーフ。memo / plan_id / etf_code / trade_type で既存取引を検知 |

両層を組み合わせることで、cronのリトライ・手動再実行・マーカー削除のいずれが起きても二重POSTを防止する。

### dry-run / execute 切替

| 環境変数 | フラグ | 動作 |
|---------|-------|------|
| `DEMO_TRADE_POST_ENABLED` 未設定 or `0` | `--dry-run` 強制 | ログ出力のみ。POSTもDB変更もマーカー作成も行わない |
| `DEMO_TRADE_POST_ENABLED=1` | `--execute` 許容 | 実POSTを実行。`--dry-run` と `--execute` は排他必須（両方/両無は exit 1） |
| 本番環境 | `--confirm-production` 必須 | `--env prod --execute` には明示的な追加フラグが必要 |

### 失敗時の継続ポリシー

- **取引単位で継続**: あるレコードがPOST失敗しても、後続レコードの処理は継続する（早期 abort しない）
- **exit code**:
  - `0`: 全成功 / dry-run正常 / trades[] 空 / JSON欠落 / マーカー既存
  - `2`: 部分成功（1件以上成功＋1件以上失敗）
  - `1`: 全失敗 / 設定エラー（`--env prod --execute` で `--confirm-production` 欠如 / ユーザー不在 など）
- batch_monitor が exit code 1/2 を検知してリトライ対象とする

### ログ出力

| 出力先 | 内容 |
|--------|------|
| `logs/execute_demo_trades.log` | スクリプト自身の詳細ログ（INFO/WARN/ERROR） |
| stdout | cron-portfolio-analysis.sh のリダイレクト先に流れる |
| `batch_logs` テーブル | `name=demo_trade_post` で実行サマリ記録（status / total_count / started_at / finished_at / error_message） |

### 通知（ポートフォリオ変更レポートメール）

POST 完了後、`backend/src/services/demo_portfolio_notifier.py:notify()` を main 末尾（batch_logs 更新・マーカー作成の後）で呼び出し、demo ユーザーのポートフォリオ変更レポートを `NOTIFY_TO_EMAIL` 宛に送信する。

| 環境変数 | 値 | 動作 |
|---------|----|------|
| `DEMO_PORTFOLIO_REPORT_ENABLED` | `0`（既定） | 送信しない（1段目フェイルセーフ） |
| `DEMO_PORTFOLIO_REPORT_ENABLED` | `1` | trigger 判定に従って送信 |
| `DEMO_PORTFOLIO_REPORT_TRIGGER` | `on_trade`（既定） | 実 POST 実行かつ取引件数 > 0 のみ送信 |
| `DEMO_PORTFOLIO_REPORT_TRIGGER` | `always` | dry-run でも送信（テスト用） |
| `DEMO_PORTFOLIO_REPORT_TRIGGER` | `never` | 送信しない |

- **宛先**: `NOTIFY_TO_EMAIL`（Daily Advisor 系と共有）
- **SMTP 失敗時の継続性**: `notify()` 内部で例外捕捉。`execute_demo_trades.py` の exit code は通知失敗の影響を受けない（WARN ログのみ）
- **件名・本文構造の詳細仕様**: [`06b_メール通知仕様.md`](06b_メール通知仕様.md#デモユーザー向けポートフォリオ変更通知) を参照

## 7. 障害時対応

### 自動リトライ（batch_monitor）

`batch_monitor.py` が5分間隔で以下を実行:

1. **タイムアウト検知**: 10分以上ハートビート更新がない `running` ジョブを `failed` に更新
2. **自動リトライ**: 直近5-60分で `failed` になったジョブを `--resume` 付きで再実行
3. **リトライ上限**: 最大3回まで（`retry_count >= 3` でスキップ）

### リトライ対象バッチ

| バッチ名 | resume機能 | リトライ時オプション |
|---------|-----------|-------------------|
| `update_etf_data` | あり（途中再開） | `--smart --rate-limit 3.0 --resume ID` |
| `update_scores` | あり（途中再開） | `--resume ID`（依存チェック自動スキップ） |
| `sync_etf_from_jpx` | あり（最初から再実行） | `--resume ID` |

### 全件失敗の検知（status の補正）

ネットワーク断などで「処理を起動できたがすべての個別アイテムが失敗した」ケースを `status='success'` のまま記録すると、`has_succeeded_today.py` が当日成功と誤判定し、catch-up 機構でも救えなくなる。これを防ぐため `BaseBatchScript._finish_batch_log` に**全件失敗ガード**を実装している。

**判定ロジック**:

| 条件 | status |
|------|--------|
| 呼び出し側が `success=False` を指定 | `failed`（変更なし） |
| `total_count > 0` かつ `success_count == 0` | `failed`（**強制補正**） |
| それ以外（部分成功含む） | `success`（呼び出し側の指定どおり） |

**仕様**:

- 呼び出し側（個別バッチ）が `_finish_batch_log(success=True, success_count=N, total_count=M)` を渡したときのみ判定が走る
- `success_count` / `total_count` を渡さない場合は従来挙動を維持（例: `daily_advisor_*` は処理件数概念がないため対象外）
- 部分成功（例: 206/454）は `success` のまま維持し、誤検知を避ける
- 全件失敗時は `error_message` に "All N items failed (0 success). Marking batch as failed." が記録される

**対象バッチ**:

| バッチ名 | 全件失敗ガード | 備考 |
|---------|--------------|------|
| `sync_from_minkabu` | あり | 0/N でDNS失敗時など |
| `update_etf_data` | あり | `success + skipped` を成功扱い |
| その他 | なし | 処理件数概念がないか、個別レポートで十分 |

**設計判断**:

- `BaseBatchScript` 経由のオプトイン方式を採用（個別バッチが `success_count` / `total_count` を明示的に渡したときだけ判定）
- 既存呼び出し（引数なし）は完全に挙動が維持されるため、advisor 系など処理件数概念のないバッチへの影響なし
- 「処理件数 > 0 かつ 成功件数 = 0」という閾値のみで判定。部分成功は健全とみなし誤検知を避ける（YAGNI）

### 依存チェックと障害時の挙動

- **`--resume` 時**: 依存チェックは自動的にバイパスされる。前回実行時に依存が充足されていたため、再チェックは不要
- **`--skip-dep-check` 時**: 月曜cronのようにチェーン実行（`&&`）で直前バッチの成功が保証される場合に使用。依存チェックを明示的にスキップする
- **依存未充足による未実行**: `update_scores.py` は10分間隔で依存チェックを行うため、依存バッチの完了後に自動的に実行される。依存バッチが失敗し続ける場合は `batch_monitor.py` が依存バッチのリトライを行い、成功後の次のcron周期で `update_scores.py` が実行される

### Daily Advisor 朝/夕のデータフロー（2026-05-14 朝/夕構成リバランス）

evening kind を「翌営業日の発注材料を完備した判断完了レポート」に再定義し、終値確定後にしか算出できない銘柄別配分・売買プランを夕方に集約した。morning kind は「寄り付き前のアクション提示」に軽量化し、前夜の決定事項を JSON 経由で受け取る。

```
16:00（平日）            update_etf_data.py          終値データ取得・performance_cache 更新
   ↓
17:30（平日）            daily_advisor_evening.py   ・終値ベースで銘柄別配分・売却/買付プラン算出
                                                    ・メール送信（【終値ベース】Daily Advisor / M/D）
                                                    ・末尾で reports/test/daily-tasks/
                                                       evening_summary_YYYYMMDD.json を永続化
   ↓
（翌営業日）07:00         daily_advisor_morning.py   ・overnight 市況サマリ取得（yfinance:S&P500/NASDAQ/DOW/USDJPY/VIX/NKD=F）
                                                    ・前夜サマリ JSON 読込（当日 → 直近営業日まで最大 5 日前 fallback）
                                                    ・メール送信（【寄り付き前】Daily Advisor / M/D）
```

- 永続化 JSON のスキーマ・読込ロジック・失敗時挙動の詳細は [`06b_メール通知仕様.md`](06b_メール通知仕様.md) の「永続化ファイル: 前夜サマリ JSON」章を参照
- 失敗時のフェイルセーフ: overnight 取得失敗 → 朝メール overnight セクションのみ skip／前夜サマリ JSON 欠落 → 朝メール リマインダーセクションのみ skip／evening のリバランス計算失敗 → evening の該当セクションのみ skip。いずれもメール本体は送信継続

### 手動対応

バッチが繰り返し失敗する場合:

```bash
# ログを確認
tail -100 logs/etf_update.log

# 手動で再実行（本番環境）
cd ~/www/japan-etf-analyzer
source backend/venv/bin/activate
python backend/scripts/update_etf_data.py --smart --rate-limit 3.0

# dry-runで事前確認
python backend/scripts/update_etf_data.py --smart --dry-run
```

### 当日キャッチアップ機構

`batch_monitor` は「**発火後に失敗したジョブ**」を3回までリトライするが、マシン停止やネットワーク断で「**発火そのものを逃したジョブ**」はカバーしない。この穴を埋めるのが **当日キャッチアップ機構** である。

#### アーキテクチャ: pull型 sweep

```
*/5 host cron → cron-batch.sh
                 ├─ 既存: 時刻ピッタリの dispatch（無変更）
                 └─ 新規: catch_up_sweep（末尾で1回）
                       → CATCHUP_JOBS の各エントリを評価し発火可否を判定
```

cron-batch.sh の `catch_up_sweep` 関数が `CATCHUP_JOBS` 配列（バッチ名・sched・until・曜日・プロファイル）を順に評価し、以下の全条件を満たすバッチを発火する:

1. 現在時刻が `sched` を**超過**（=ピッタリは通常 dispatch に委譲・重複防止）し、`until` 以下
2. 曜日条件OK（weekday / daily / mon / fri / sun）
3. 祝日OK（既存 dispatch と整合: 平日対象は祝日スキップ、月曜マスタチェーンと金曜weeklyは祝日でも実行）
4. プロファイル一致（`CRON_BATCH_PROFILE` = dev/prod、または both）
5. `backend/scripts/has_succeeded_today.py NAME` が exit 1 を返す（当日まだ成功記録なし）

#### 設計判断

| 論点 | 採用 | 理由 |
|------|------|------|
| 連鎖の扱い | **次の */5 サイクルに委ねる**（fixed-point ループなし） | 前提バッチ完了 → 5分後 sweep で後続が条件を満たし発火。当日中なら最大5分遅延は許容。実装単純化（YAGNI） |
| 遡り範囲 | 当日のみ（JST 0:00-23:59） | `get_latest_success_time` の参照基準が JST 0:00 リセットなので自然な実装。翌日持ち越しは別問題なので分離 |
| 高頻度バッチ（*/5・*/10） | catch-up 対象外 | 短期サイクル内で自然リカバリされる。catch-up 不要 |
| 通知系の窓 | `until` 超過でスキップ | 古い情報を朝7時用テンプレで送ると害になるため、時間帯を過ぎたら諦める方針 |
| アーキテクチャ | pull 型 sweep | push 型（バッチごとに発火タイマー）と比べ、起動経路が一本化されデバッグ容易 |

#### 対象バッチ一覧

| バッチ | sched | until | 曜日 | プロファイル | 連鎖発火 |
|-------|-------|-------|------|------------|---------|
| `update_etf_data` | 16:00 | 22:00 | 平日（祝日スキップ） | both | — |
| `sync_from_minkabu` | 16:00 | 22:00 | 平日（祝日スキップ） | both | — |
| `daily_advisor_morning` | 07:00 | 09:00 | 平日（祝日スキップ） | dev | — |
| `daily_advisor_evening` | 17:30 | 22:00 | 平日（祝日スキップ） | dev | depends_on で update_etf_data 等を待つ |
| `etf_rating_daily` | 18:15 | 22:00 | 平日（祝日スキップ） | dev | `update_etf_data` / `sync_from_minkabu` の終値データに依存。通常起動は月-木 18:00 / 金 18:15（weekly との並列回避）、catch-up sched は曜日全体の最遅時刻 18:15（金曜の通常起動との二重発火防止、月-木はマシン停止時に最大15分遅延で復旧）|
| `daily_advisor_weekly` | 18:00 | 22:00 | 金（祝日でも実行） | dev | — |
| `update_theme_etfs` | 03:00 | 23:59 | 日 | dev | — |
| `rotate_logs` | 05:00 | 23:59 | 毎日 | both | — |

**対象外と理由**:

| 対象外バッチ | 理由 |
|------------|------|
| `batch_monitor`（*/5） | 高頻度。短期サイクル内で自然リカバリ |
| `update_scores`（*/10 16-20） | 同上。16:00〜20:00 で10分おきに何度も発火 |
| `mechanical_rule_watcher`（*/5 9-15） | 同上。場中5分おきに発火 |
| `sync_etf_from_jpx` | 月曜マスタチェーン1段目。`run_chain` 同期実行が前提のため個別 catch-up は不適 |
| `update_scores_master` | 同2段目。実体 `update_scores.py` の `check_window=(16:30, 22:00)` により午前帯発火がほぼ skip される。また `batch_logs.name` は `update_scores` で記録されるため `update_scores_master` 名での「当日成功記録」判定が常に未成功扱いとなり、毎サイクル発火試行が発生する |
| `sync_historical_splits` | 同3段目。`depends_on` を持たないため、`sync_etf_from_jpx` 未完了でも並列発火するリスクあり |

月曜マスタチェーン障害時は **手動運用**で対応する:

```bash
# 個別に順次再実行（直前バッチ完了を確認しながら）
bash scripts/cron-batch.sh --only sync_etf_from_jpx
bash scripts/cron-batch.sh --only update_scores_master
bash scripts/cron-batch.sh --only sync_historical_splits
```

#### 自己防衛

catch-up sweep が無条件で起動しても、各バッチ側で安全に止まる:

- `BaseBatchScript._check_dependencies()`: `depends_on` の前提未充足なら exit 0（skip）で終了。catch-up が暴走しても二重起動・データ破壊は起きない
- `flock`（`/tmp/cron-batch-<NAME>.lock`）: 通常 dispatch との同時起動を防止
- `has_succeeded_today.py` が exit 2（エラー）を返す場合は安全側に倒して catch-up 対象外とする

#### 重複防止

`should_catchup` の時刻判定は **`sched < now`**（厳密超過）で行うため、sched ピッタリの発火（例: 16:00）は通常 dispatch が独占的に拾う。catch-up は16:05以降から候補に上がる。

#### 検証方法

```bash
# 16:00を逃した想定（19:00時点）
bash scripts/cron-batch.sh --at 19:00 --dow 3 --dry-run
# 期待: update_etf_data, sync_from_minkabu, daily_advisor_evening が CATCHUP 候補

# 朝のmorning窓外スキップ
bash scripts/cron-batch.sh --at 10:00 --dow 3 --dry-run
# 期待: daily_advisor_morning は窓外（until=09:00 超過）で対象外

# 月曜マスタチェーンは catch-up 対象外
bash scripts/cron-batch.sh --at 12:00 --dow 1 --dry-run
# 期待: マスタチェーン3バッチは CATCHUP 候補に現れない（rotate_logs のみ）

# 単一バッチの当日成功有無を確認
docker compose exec -T backend python3 scripts/has_succeeded_today.py update_etf_data
# 0=成功済 / 1=未成功 / 2=エラー
```
