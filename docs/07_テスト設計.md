# テスト設計書

具体的なテスト実装例は `07a_テストケース詳細.md` を参照。

## 1. テスト戦略

### 1.1 基本方針

| 項目 | 方針 |
|------|------|
| 開発手法 | TDD（テスト駆動開発） |
| カバレッジ目標 | 70%以上 |
| テストフレームワーク | pytest（Backend）、Vitest（Frontend） |

### 1.2 テストピラミッド

```
                    ┌─────────────┐
                    │   E2E Test  │  少数・高コスト
                    │   (手動)    │
                    ├─────────────┤
                ┌───┴─────────────┴───┐
                │   Integration Test  │  中程度
                │  (API・コンポーネント) │
                ├─────────────────────┤
            ┌───┴─────────────────────┴───┐
            │        Unit Test           │  多数・低コスト
            │  (Service・Repository・Hook) │
            └─────────────────────────────┘
```

### 1.3 テストレベル

| レベル | 対象 | ツール | 実行タイミング |
|--------|------|--------|----------------|
| 単体テスト | Service, Repository, Hook | pytest, Vitest | コミット前 |
| 統合テスト | API, Component | pytest, Vitest | プッシュ前 |
| E2Eテスト | 画面操作 | 手動 | リリース前 |

## 2. テスト構成

### 2.1 バックエンド

```
backend/tests/
├── conftest.py               # テスト共通設定
├── unit/                     # 単体テスト
│   ├── services/
│   │   ├── test_recommend_service.py
│   │   ├── test_etf_service.py
│   │   ├── test_chart_service.py
│   │   └── test_search_service.py
│   ├── repositories/
│   │   ├── test_etf_repository.py
│   │   └── test_category_repository.py
│   └── utils/
│       └── test_validators.py
├── integration/              # 統合テスト
│   └── routes/
│       ├── test_recommend_routes.py
│       ├── test_etf_routes.py
│       ├── test_category_routes.py
│       └── test_tag_routes.py
└── fixtures/                 # テストデータ
    ├── etfs.json
    ├── categories.json
    └── perspectives.json
```

### 2.2 フロントエンド

```
frontend/src/
├── __tests__/                # グローバルテスト設定
│   └── setup.ts
├── components/
│   ├── search/
│   │   ├── SearchBar.tsx
│   │   └── SearchBar.test.tsx
│   ├── etf/
│   │   ├── ETFCard.tsx
│   │   └── ETFCard.test.tsx
│   └── chart/
│       ├── PriceChart.tsx
│       └── PriceChart.test.tsx
├── hooks/
│   ├── useETFSearch.ts
│   └── useETFSearch.test.ts
└── api/
    ├── etf.ts
    └── etf.test.ts
```

## 3. テスト対象機能一覧

### 3.1 バックエンド

| 機能ID | 機能名 | テスト対象 | テスト種別 |
|--------|--------|-----------|-----------|
| F-001 | おすすめ提案 | RecommendService.get_recommendations | 単体 |
| F-001 | おすすめ提案 | GET /api/v1/recommendations | 統合 |
| F-001 | おすすめ提案 | GET /api/v1/perspectives | 統合 |
| F-002 | 銘柄検索 | ETFService.search | 単体 |
| F-002 | 銘柄検索 | GET /api/v1/etfs | 統合 |
| F-003 | 銘柄詳細 | ETFService.get_by_code | 単体 |
| F-003 | 銘柄詳細 | GET /api/v1/etfs/{code} | 統合 |
| F-004 | チャート | ChartService.get_chart | 単体 |
| F-004 | チャート | GET /api/v1/etfs/{code}/chart | 統合 |
| - | カテゴリ | GET /api/v1/categories | 統合 |
| - | タグ | GET /api/v1/tags | 統合 |
| - | demo取引POST自動化 | `backend/scripts/execute_demo_trades.py`（cron Step 7） | 単体（13ケース） |

#### demo取引POST自動化のテスト（`backend/tests/test_execute_demo_trades.py`）

`execute_demo_trades.py` の単体テスト（全13ケース・PASS）。`reports/demo/trades_execution_plan.json` → `POST /api/v1/demo/trades` の自動投入フローを保護する。

| 区分 | ケース数 | 内容 |
|------|--------|------|
| helper関数 | 5件 | `build_payload`（plan_id 有/無で memo 組立分岐）、`is_duplicate`（plan_id 一致 / `[auto]` memo 一致 / 不一致）、exit code 算出ロジック × 3パターン（全成功/部分成功/全失敗） |
| main関数 | 8件 | trades[] 空配列で exit 0、DB重複時スキップ、`--dry-run` 既定動作、本番POSTの `--confirm-production` 必須チェック、マーカーによる early-exit、JSON欠落時 exit 0、部分失敗時 exit 2、plan_id null レコードの memo `[auto]` プレフィックス処理 |

### 3.2 フロントエンド

| 機能ID | 機能名 | テスト対象 | テスト種別 |
|--------|--------|-----------|-----------|
| F-001 | おすすめ提案 | RecommendSection | コンポーネント |
| F-001 | おすすめ提案 | PerspectiveTabs | コンポーネント |
| F-001 | おすすめ提案 | useRecommendations | フック |
| F-002 | 銘柄検索 | SearchBar | コンポーネント |
| F-002 | 銘柄検索 | FilterPanel | コンポーネント |
| F-002 | 銘柄検索 | useETFSearch | フック |
| F-003 | 銘柄詳細 | ETFDetailModal | コンポーネント |
| F-003 | 銘柄詳細 | MetricTable | コンポーネント |
| F-004 | チャート | PriceChart | コンポーネント |
| F-004 | チャート | useChartData | フック |
| F-005 | お気に入り | FavoriteButton | コンポーネント |
| F-005 | お気に入り | useFavorites | フック |

### 3.3 メールテンプレート（Daily Advisor）

Daily Advisor の通知メール（morning / evening / weekly / alert）は、テンプレート + レンダラの組み合わせで生成されるため、文言の微小変更でも本番出力が壊れる。これを防ぐために、ユニットテストに加えて**スナップショットテスト**で回帰防止する。期待値は `backend/tests/unit/services/__snapshots__/{kind}_{tag}.{md,html}` にゴールデンとして配置し、`test_notification_renderer_snapshot.py` が 11 ケース × md/html を完全一致比較する。仕様変更時は `SNAPSHOT_UPDATE=1 pytest ...` で期待値を再生成し、Git 差分を目視レビューしてからコミットする。外部ライブラリ非依存（手書き比較）。詳細は [`06b_メール通知仕様.md`](06b_メール通知仕様.md) を参照。

## 4. E2Eテスト（手動）

### 4.1 テストシナリオ

| No | シナリオ | 手順 | 期待結果 |
|----|----------|------|----------|
| 1 | おすすめ表示 | トップページを開く | 高配当のおすすめ銘柄が表示される |
| 2 | 観点切替 | 「低コスト」タブをクリック | 低コストのおすすめ銘柄に切り替わる |
| 3 | おすすめから詳細 | おすすめカードをクリック | 銘柄詳細モーダルが表示される |
| 4 | 基本検索 | 検索バーでキーワード入力 | 該当銘柄が一覧に表示される |
| 5 | カテゴリ絞込 | カテゴリを選択 | 該当カテゴリのみ表示 |
| 6 | タグ絞込 | タグをクリック | 該当タグを持つ銘柄のみ表示 |
| 7 | 一覧から詳細 | 一覧のカードをクリック | 銘柄詳細モーダルが表示される |
| 8 | チャート表示 | モーダル内で期間選択 | 選択期間のチャートが表示 |
| 9 | モーダルを閉じる | ×ボタンまたはオーバーレイクリック | モーダルが閉じ、一覧に戻る |
| 10 | お気に入り登録 | 一覧の☆ボタンをクリック | ★に変化し、登録される |
| 11 | お気に入り解除 | 一覧の★ボタンをクリック | ☆に変化し、解除される |
| 12 | モーダル内お気に入り | モーダル内の★/☆ボタンをクリック | 状態がトグルされ、一覧にも反映 |

### 4.2 リリース前チェックリスト

- [ ] 全機能が正常に動作する
- [ ] レスポンシブ表示が正常（PC/タブレット/モバイル）
- [ ] エラー時に適切なメッセージが表示される
- [ ] ローディング表示が適切
- [ ] 本番環境でAPIが正常に動作する

## 5. テスト実行コマンド

### 5.1 バックエンド

```bash
# 全テスト実行
docker compose exec backend pytest

# 単体テストのみ
docker compose exec backend pytest tests/unit/

# 統合テストのみ
docker compose exec backend pytest tests/integration/

# カバレッジ計測
docker compose exec backend pytest --cov=src --cov-report=html

# 特定テスト実行
docker compose exec backend pytest tests/unit/services/test_etf_service.py -v
```

### 5.2 フロントエンド

```bash
# 全テスト実行
docker compose exec frontend npm test

# ウォッチモード
docker compose exec frontend npm run test:watch

# カバレッジ計測
docker compose exec frontend npm run test:coverage

# 特定ファイルのテスト
docker compose exec frontend npm test -- SearchBar.test.tsx
```

## 6. CI/CD（将来）

### 6.1 GitHub Actions設定例

```yaml
# .github/workflows/test.yml

name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: |
          cd backend
          pytest --cov=src --cov-report=xml
      - name: Upload coverage
        uses: codecov/codecov-action@v3

  frontend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: |
          cd frontend
          npm ci
      - name: Run tests
        run: |
          cd frontend
          npm run test:coverage
```

### 6.2 コミット前フック（pre-commit）

```yaml
# .pre-commit-config.yaml

repos:
  - repo: local
    hooks:
      - id: backend-test
        name: Backend Tests
        entry: docker compose exec -T backend pytest
        language: system
        pass_filenames: false

      - id: frontend-test
        name: Frontend Tests
        entry: docker compose exec -T frontend npm test
        language: system
        pass_filenames: false
```
